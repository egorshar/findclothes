define('app/common/search_item', function ($) {
  var Backbone = require('backbone'),
      $ = require('jquery'),
      SearchItem;

  SearchItem = Backbone.View.extend({
    initialize: function () {

    }
  });

  return SearchItem;
});
